###############################################################################
# Tiresias Platform — Root Docker Compose
#
# Profiles:
#   (default) — db + mailhog + platform-api + platform-web
#   full       — all of the above + redis + platform-app-proxy + platform-sovereign + worker
#   ci         — db only (services built & tested separately in CI)
#
# Usage:
#   docker compose up --build                  # default profile
#   docker compose --profile full up --build   # full stack
#   docker compose --profile ci up             # CI infra only
###############################################################################

services:

  # ── Infrastructure ──────────────────────────────────────────────────────────

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-platform}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-platform}
      POSTGRES_DB: ${POSTGRES_DB:-platform}
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-platform} -d ${POSTGRES_DB:-platform}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  mailhog:
    image: mailhog/mailhog:latest
    restart: unless-stopped
    profiles: [default, full]
    ports:
      - "1025:1025"  # SMTP
      - "8025:8025"  # Web UI
    logging:
      driver: none

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    profiles: [full]
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

  # ── Applications ────────────────────────────────────────────────────────────

  platform-api:
    build:
      context: .
      dockerfile: apps/platform-api/Dockerfile
    restart: unless-stopped
    profiles: [default, full]
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-platform}:${POSTGRES_PASSWORD:-platform}@db:5432/${POSTGRES_DB:-platform}
      SESSION_SECRET: ${SESSION_SECRET:-changeme-at-least-32-chars-long!!}
      AUTH_MODE: ${AUTH_MODE:-local}
      WEB_PUBLIC_URL: ${WEB_PUBLIC_URL:-http://localhost:3000}
      API_PUBLIC_URL: ${API_PUBLIC_URL:-http://localhost:8000}
      LOG_LEVEL: ${LOG_LEVEL:-info}
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/health/live')\""]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  platform-web:
    build:
      context: .
      dockerfile: apps/platform-web/Dockerfile
    restart: unless-stopped
    profiles: [default, full]
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-platform}:${POSTGRES_PASSWORD:-platform}@db:5432/${POSTGRES_DB:-platform}
      SESSION_SECRET: ${SESSION_SECRET:-changeme-at-least-32-chars-long!!}
      AUTH_MODE: ${AUTH_MODE:-local}
      WEB_PUBLIC_URL: ${WEB_PUBLIC_URL:-http://localhost:3000}
      API_PUBLIC_URL: ${API_PUBLIC_URL:-http://localhost:8000}
      SMTP_HOST: mailhog
      SMTP_PORT: "1025"
      NODE_ENV: production
    depends_on:
      platform-api:
        condition: service_healthy
    ports:
      - "3000:3000"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/health/live || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  platform-app-proxy:
    build:
      context: apps/platform-app-proxy
      dockerfile: Dockerfile
    restart: unless-stopped
    profiles: [full]
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-platform}:${POSTGRES_PASSWORD:-platform}@db:5432/${POSTGRES_DB:-platform}
      API_URL: http://platform-api:8000
    depends_on:
      platform-api:
        condition: service_healthy
    ports:
      - "8080:8080"

  platform-sovereign:
    build:
      context: apps/platform-sovereign
      dockerfile: Dockerfile
    restart: unless-stopped
    profiles: [full]
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-platform}:${POSTGRES_PASSWORD:-platform}@db:5432/${POSTGRES_DB:-platform}
      API_URL: http://platform-api:8000
    depends_on:
      platform-api:
        condition: service_healthy
    ports:
      - "8090:8090"

  worker:
    build:
      context: apps/platform-api
      dockerfile: Dockerfile
    restart: unless-stopped
    profiles: [full]
    command: ["python", "-m", "src.worker"]
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-platform}:${POSTGRES_PASSWORD:-platform}@db:5432/${POSTGRES_DB:-platform}
      REDIS_URL: redis://redis:6379/0
      SESSION_SECRET: ${SESSION_SECRET:-changeme-at-least-32-chars-long!!}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  db_data:
