---
phase: 17-aletheia-dashboard-tier-gating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - portal/src/components/dashboard/DashboardSidebar.tsx
  - portal/src/components/dashboard/DashboardHeader.tsx
  - portal/src/app/dashboard/aletheia/page.tsx
  - portal/src/app/dashboard/aletheia/cot-audit/page.tsx
  - portal/src/app/dashboard/aletheia/tool-activity/page.tsx
  - portal/src/app/dashboard/aletheia/sanitizer/page.tsx
  - portal/src/app/dashboard/aletheia/policies/page.tsx
autonomous: true
requirements: [ALETH-13, ALETH-14]

must_haves:
  truths:
    - "All /v1/aletheia/* endpoints return 402 for community/starter/pro tiers"
    - "Aletheia nav group appears in sidebar for enterprise/mssp/saas tiers only"
    - "Overview page shows CoT chain health, tool invocation timeline, sanitizer verdicts, and policy violations"
    - "CoT Audit page shows chain entries, integrity status, proof export, and content viewer gated to audit role"
    - "Tool Activity page shows invocation timeline, command frequency, agent heatmap, and deny/block log"
    - "Sanitizer page shows verdict distribution, pattern match frequency, and blocked response forensics"
    - "Policy Editor page shows YAML editor, evaluation simulator, and audit log"
  artifacts:
    - path: "portal/src/app/dashboard/aletheia/page.tsx"
      provides: "Aletheia Overview dashboard"
      min_lines: 120
    - path: "portal/src/app/dashboard/aletheia/cot-audit/page.tsx"
      provides: "CoT Audit page"
      min_lines: 100
    - path: "portal/src/app/dashboard/aletheia/tool-activity/page.tsx"
      provides: "Tool Activity page"
      min_lines: 100
    - path: "portal/src/app/dashboard/aletheia/sanitizer/page.tsx"
      provides: "Sanitizer page"
      min_lines: 100
    - path: "portal/src/app/dashboard/aletheia/policies/page.tsx"
      provides: "Policy Editor page"
      min_lines: 100
  key_links:
    - from: "portal/src/app/dashboard/aletheia/page.tsx"
      to: "/v1/aletheia/cot/chain, /watch/v1/aletheia/tools/invocations"
      via: "useWidgetData hook"
      pattern: "useWidgetData.*aletheia"
    - from: "portal/src/app/dashboard/aletheia/cot-audit/page.tsx"
      to: "/v1/aletheia/cot/chain, /v1/aletheia/cot/chain/{id}/content, /v1/aletheia/cot/proof"
      via: "useWidgetData and api.post"
      pattern: "aletheia/cot"
    - from: "portal/src/app/dashboard/aletheia/policies/page.tsx"
      to: "/v1/aletheia/tool/evaluate, /v1/aletheia/tool/reload"
      via: "api.post"
      pattern: "aletheia/tool"
    - from: "portal/src/components/dashboard/DashboardSidebar.tsx"
      to: "TierGate logic"
      via: "aletheia nav group conditional render"
      pattern: "group.*aletheia"
---

<objective>
Build the Aletheia dashboard UI: verify tier gating returns 402 for sub-enterprise tiers on all /v1/aletheia/* endpoints, add an "Aletheia" nav group to the sidebar (visible only to enterprise+), create 5 Aletheia dashboard pages (Overview, CoT Audit, Tool Activity, Sanitizer, Policy Editor), and add an Aletheia status indicator to DashboardHeader.

Purpose: ALETH-13 ensures Aletheia features are commercially gated. ALETH-14 gives security analysts a visual interface for all Aletheia capabilities without needing API calls.
Output: 5 new dashboard pages under /dashboard/aletheia/*, sidebar nav group, header indicator, tier gating verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key interfaces the executor needs:

<interfaces>
From portal/src/lib/useWidgetData.ts:
```typescript
interface UseWidgetDataOptions<T> {
  endpoint: string;
  transform?: (raw: unknown) => T;
  refreshInterval?: number;
  skip?: boolean;
}
function useWidgetData<T>(opts: UseWidgetDataOptions<T>): { data: T | null; loading: boolean; error: string | null; refetch: () => void; }
```

From portal/src/components/dashboard/TierGate.tsx:
```typescript
type Tier = "community" | "starter" | "pro" | "enterprise" | "mssp" | "saas";
function tierMeets(actualTier: string, requiredTier: Tier): boolean;
function TierGate({ requiredTier, featureLabel, children }: TierGateProps): JSX.Element;
```

From portal/src/lib/auth.ts:
```typescript
interface AuthSession { soulkey: string; tenant_id: string; persona_id: string; tier: string; tenant_name?: string; expires_at: number; }
function useAuth(): { session: AuthSession | null; loading: boolean; login: (soulkey: string) => Promise<void>; logout: () => void; error: string | null; }
```

From portal/src/lib/api.ts:
```typescript
const api = { get(endpoint), post(endpoint, body), put(endpoint, body), delete(endpoint) }
```
</interfaces>

Backend Aletheia endpoints (all gated to enterprise+ in FeatureGateMiddleware via FEATURE_MIN_TIER):
- GET  /v1/aletheia/cot/chain?limit=N&offset=N -- list chain entries
- GET  /v1/aletheia/cot/chain/{request_id} -- single chain entry
- GET  /v1/aletheia/cot/chain/{request_id}/content -- encrypted content (audit role)
- POST /v1/aletheia/cot/chain/verify -- verify chain integrity over range
- POST /v1/aletheia/cot/proof -- export hashed proof document
- POST /v1/aletheia/tool/evaluate -- evaluate tool invocation against policies
- POST /v1/aletheia/tool/reload -- hot-reload policies
- POST /v1/aletheia/sanitize -- sanitize tool output

SoulWatch endpoints (tool invocations from Phase 14):
- GET  /watch/v1/aletheia/tools/invocations?limit=N -- list tool invocations
- GET  /watch/v1/aletheia/tools/invocations/{id} -- invocation detail
- GET  /watch/v1/aletheia/tools/invocations/stats -- command frequency + agent summary

Feature gate entries already exist in src/middleware/feature_gate.py:
- aletheia_cot_intercept: enterprise
- aletheia_tool_monitoring: enterprise
- aletheia_response_sanitizer: enterprise
- aletheia_tool_policies: enterprise
- aletheia_dashboard: enterprise

Sidebar nav pattern: NAV_ITEMS array with {label, href, icon, group} objects. Groups rendered via BASE_GROUPS array. MSSP group conditionally added when tier is mssp/saas. Use same conditional pattern for Aletheia group -- render when tierMeets(tier, "enterprise").

DashboardHeader PAGE_TITLES dict maps route paths to display titles.

Obsidian Flux CSS tokens: --of-primary (#5adace), --of-background (#121318), --of-surface-container, --of-surface-container-high, --of-surface-container-highest, --of-on-surface, --of-on-surface-variant, --of-outline-variant, --of-error, --of-accent.

Existing dashboard page pattern (from detection/page.tsx, mssp/page.tsx):
- "use client" directive
- useWidgetData for data fetching (30s refresh)
- CSS-div based charts (no chart library -- use Tailwind width/height for bars)
- Severity/status color tokens from Obsidian Flux
- Stat cards with icon, label, value, loading skeleton pattern
- TierGate wrapper for tier-restricted content
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify tier gating + add Aletheia sidebar nav group and header entries</name>
  <files>
    portal/src/components/dashboard/DashboardSidebar.tsx
    portal/src/components/dashboard/DashboardHeader.tsx
  </files>
  <action>
**Tier gating verification (ALETH-13):**
Verify by reading src/middleware/feature_gate.py and confirming all aletheia features map to "enterprise" minimum tier in FEATURE_MIN_TIER dict. The FeatureGateMiddleware already returns 402 for sub-enterprise tiers on any /v1/aletheia/* route. The ROUTE_FEATURES dict maps "/v1/aletheia/tool" to "aletheia_tool_policies" and "/v1/aletheia" to "aletheia_cot_intercept" -- both set to "enterprise". Additionally verify "aletheia_response_sanitizer", "aletheia_tool_monitoring", "aletheia_dashboard" all map to "enterprise". Document this in the summary as ALETH-13 verified.

If you can start the backend (cd /home/cristian/tiresias && python -m uvicorn src.main:app --port 8100), also run curl tests:
```
curl -s -o /dev/null -w "%{http_code}" http://localhost:8100/v1/aletheia/cot/chain -H "Authorization: Bearer <pro_key>"
```
Expected: 402. But code inspection is sufficient if backend cannot be started.

**Sidebar nav group (ALETH-14):**
In DashboardSidebar.tsx:

1. Import tierMeets from "@/components/dashboard/TierGate".
2. Import lucide icons: Eye, Link2, Terminal, ShieldCheck, FileCode.
3. Add ALETHEIA_GROUP constant:
   ```
   const ALETHEIA_GROUP = { key: "aletheia" as const, label: "Aletheia" };
   ```
4. Update GroupKey type to include "aletheia".
5. In the groups computation, add ALETHEIA_GROUP when tierMeets(session?.tier ?? "community", "enterprise"):
   ```
   const isEnterprisePlus = tierMeets(session?.tier ?? "community", "enterprise");
   ```
   Insert ALETHEIA_GROUP between the "security" and "soulwatch" groups when isEnterprisePlus is true.
6. Add 5 NAV_ITEMS with group: "aletheia":
   - { label: "Overview", href: "/dashboard/aletheia", group: "aletheia", icon: <Eye className="w-5 h-5" /> }
   - { label: "CoT Audit", href: "/dashboard/aletheia/cot-audit", group: "aletheia", icon: <Link2 className="w-5 h-5" /> }
   - { label: "Tool Activity", href: "/dashboard/aletheia/tool-activity", group: "aletheia", icon: <Terminal className="w-5 h-5" /> }
   - { label: "Sanitizer", href: "/dashboard/aletheia/sanitizer", group: "aletheia", icon: <ShieldCheck className="w-5 h-5" /> }
   - { label: "Policies", href: "/dashboard/aletheia/policies", group: "aletheia", icon: <FileCode className="w-5 h-5" /> }

**DashboardHeader updates:**
1. Add PAGE_TITLES entries:
   - "/dashboard/aletheia": "Aletheia Overview"
   - "/dashboard/aletheia/cot-audit": "CoT Audit"
   - "/dashboard/aletheia/tool-activity": "Tool Activity"
   - "/dashboard/aletheia/sanitizer": "Sanitizer"
   - "/dashboard/aletheia/policies": "Policy Editor"

2. Add Aletheia status indicator between search and notification bell. Import tierMeets from TierGate and useWidgetData. When tierMeets(session?.tier, "enterprise"), render a small indicator:
   - Fetch /v1/aletheia/cot/chain?limit=1 via useWidgetData (60s refresh, skip when not enterprise+)
   - Green dot + "Aletheia" text when data loads successfully
   - Grey dot + "Aletheia" text when loading or error
   - Styled: flex items-center gap-1.5, dot is w-2 h-2 rounded-full, text is text-[10px] font-bold uppercase tracking-wider
  </action>
  <verify>
    <automated>cd /home/cristian/tiresias/portal && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Aletheia nav group renders in sidebar for enterprise/mssp/saas tiers, hidden for community/starter/pro
    - All 5 Aletheia page routes listed in sidebar with correct icons
    - DashboardHeader shows Aletheia status indicator for enterprise+ tiers
    - All 5 routes have PAGE_TITLES entries
    - ALETH-13 tier gating verified via code inspection of feature_gate.py
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 5 Aletheia dashboard pages</name>
  <files>
    portal/src/app/dashboard/aletheia/page.tsx
    portal/src/app/dashboard/aletheia/cot-audit/page.tsx
    portal/src/app/dashboard/aletheia/tool-activity/page.tsx
    portal/src/app/dashboard/aletheia/sanitizer/page.tsx
    portal/src/app/dashboard/aletheia/policies/page.tsx
  </files>
  <action>
Create all 5 pages following the existing dashboard page pattern (detection/page.tsx, mssp/page.tsx). Every page MUST be wrapped in TierGate requiredTier="enterprise" featureLabel="Aletheia ..." so sub-enterprise tiers see the upgrade prompt instead of broken data.

**Page 1: /dashboard/aletheia/page.tsx (Overview)**
Four widget sections in a 2x2 grid (lg:grid-cols-2, gap-6):

1. **CoT Chain Health** -- fetch GET /v1/aletheia/cot/chain?limit=10 via useWidgetData (30s). Show stat card: total entries count from response.total, latest entry timestamp, mini integrity badge (green "Healthy" / yellow "Unverified"). Card style: bg-of-surface-container rounded-xl border border-of-outline-variant/5 p-5.

2. **Tool Invocation Timeline** -- fetch GET /watch/v1/aletheia/tools/invocations?limit=20 via useWidgetData (30s). Vertical timeline list: each item shows command (font-mono text-sm), agent_id (text-of-on-surface-variant text-xs), timestamp (relative), exit_code badge (0=green, non-zero=red). Timeline styling: left-2 border-l-2 border-of-outline-variant/20, each item has a dot (-left-[5px] w-2 h-2 rounded-full).

3. **Sanitizer Verdicts** -- fetch GET /watch/v1/aletheia/tools/invocations/stats via useWidgetData (60s). Three stat cards (pass/warn/block) in a row, plus a horizontal stacked bar below. Bar segments: pass=bg-emerald-500/60, warn=bg-yellow-500/60, block=bg-of-error/60. Width proportional to count.

4. **Policy Violations** -- fetch GET /watch/v1/aletheia/tools/invocations?limit=50 via useWidgetData (30s). Filter client-side: items where policy_verdict !== "allow" and policy_verdict is not null. Table: command, agent_id, policy_rule_matched, timestamp. Empty state: ShieldCheck icon + "No policy violations detected" in text-of-on-surface-variant.

**Page 2: /dashboard/aletheia/cot-audit/page.tsx (CoT Audit)**
State: expandedRequestId (string | null), verifyResult (object | null), proofLoading (boolean).

1. **Chain Entries Table** -- fetch GET /v1/aletheia/cot/chain?limit=50 via useWidgetData (30s). Table columns: request_id (font-mono text-xs, truncate to 12 chars), model, provider, cot_token_count, timestamp. Click row sets expandedRequestId. Expanded row shows full detail + content viewer below.

2. **Integrity Verification** -- "Verify Chain" button (bg-of-primary/15 text-of-primary). On click: api.post("/v1/aletheia/cot/chain/verify", { start_index: 0, end_index: -1 }). Show result badge: valid=green "Chain Valid", invalid=red "Broken at entry N".

3. **Proof Export** -- "Export Proof" button. On click: api.post("/v1/aletheia/cot/proof", { format: "json" }). Create blob URL and trigger download: const blob = new Blob([JSON.stringify(data, null, 2)], { type: "application/json" }); window.open(URL.createObjectURL(blob)).

4. **Content Viewer** -- when expandedRequestId is set, fetch GET /v1/aletheia/cot/chain/{expandedRequestId}/content via useWidgetData (skip when null). Display content in pre block (bg-of-surface-container-high rounded-lg p-4 font-mono text-xs overflow-x-auto). Show note: "Content access requires audit role" in text-of-on-surface-variant text-[10px].

**Page 3: /dashboard/aletheia/tool-activity/page.tsx (Tool Activity)**
1. **Invocation Timeline** -- fetch /watch/v1/aletheia/tools/invocations?limit=100. Scrollable list (max-h-96 overflow-y-auto). Each card: left color stripe (4px, green if exit_code===0, red otherwise), command (font-mono), agent_id, duration_ms + "ms" suffix, timestamp. Group by hour using date headers.

2. **Command Frequency** -- fetch /watch/v1/aletheia/tools/invocations/stats. Horizontal bar chart: top 10 commands. Each bar: label on left, CSS-width bar (bg-of-primary/40), count on right. Max bar = 100% width, others proportional.

3. **Agent Heatmap** -- from stats endpoint agent summary data. CSS grid (grid-cols-4 sm:grid-cols-6 lg:grid-cols-8 gap-2). Each cell: agent_id truncated, background opacity scales with activity count (min 10% max 80% of --of-primary). Tooltip title with full agent_id and count.

4. **Deny/Block Log** -- filter invocations where policy_verdict="deny" OR sanitizer_verdict="block". Table with red left border-l-2 border-of-error/40 on each row. Columns: timestamp, command, agent_id, verdict (badge), rule/pattern matched.

**Page 4: /dashboard/aletheia/sanitizer/page.tsx (Sanitizer)**
1. **Verdict Distribution** -- fetch stats. Three large stat cards in a row: Pass (text-emerald-400, bg-emerald-500/10), Warn (text-yellow-400, bg-yellow-500/10), Block (text-of-error, bg-of-error/10). Each shows count and percentage. Below: full-width stacked bar (h-3 rounded-full overflow-hidden flex).

2. **Pattern Match Frequency** -- aggregate patterns_matched from invocations (client-side: flatten all patterns_matched arrays, count occurrences). Ranked list: pattern name (font-mono), horizontal bar (bg-of-primary/30), count. Sort descending.

3. **Blocked Response Forensics** -- fetch invocations, filter sanitizer_verdict="block". Table: timestamp, command, agent_id, patterns_matched (render as small badges px-1.5 py-0.5 rounded bg-of-error/15 text-of-error text-[10px]), stdout_hash (font-mono truncated). Expand row to show stderr_hash, duration_ms, full args. Note banner at top: "Full blocked content available via forensics API" in text-of-on-surface-variant bg-of-surface-container-high rounded-lg p-3 text-xs.

**Page 5: /dashboard/aletheia/policies/page.tsx (Policy Editor)**
State: yamlContent (string), evalResult (object | null), evalLoading (boolean).

1. **YAML Policy Editor** -- textarea (w-full min-h-[300px] bg-of-surface-container-high text-of-on-surface font-mono text-sm p-4 rounded-lg border border-of-outline-variant/10 resize-y). Pre-fill with placeholder YAML comment: "# Paste your tool policy YAML here\n# See docs for policy schema". Below: two buttons -- "Reload Policies" (POSTs /v1/aletheia/tool/reload, shows success/error inline) and "Save" (disabled, with tooltip "GET policy endpoint planned for future phase"). Info banner: "Policy files are loaded from disk. Use Reload to pick up changes." in bg-of-surface-container rounded-lg p-3 text-xs text-of-on-surface-variant.

2. **Evaluation Simulator** -- form card. Fields: agent_id (text), tenant_id (text, default from useAuth session.tenant_id), command (text), args (textarea for JSON, default "[]"), working_directory (text, default "/"). "Evaluate" button POSTs to /v1/aletheia/tool/evaluate with the form data (parse args as JSON). Result card below: verdict badge (allow=green, deny=red, audit=yellow), matched_rule (font-mono), reason text.

3. **Recent Evaluations** -- fetch /watch/v1/aletheia/tools/invocations?limit=20 and show entries that have policy_verdict set. Simple chronological list: timestamp, agent_id, command, verdict badge. If none, empty state.

**All pages shared patterns:**
- "use client" directive at top
- Wrap page content in: <TierGate requiredTier="enterprise" featureLabel="Aletheia {PageName}">
- Import useWidgetData from "@/lib/useWidgetData"
- Import api from "@/lib/api" for POST calls
- Import useAuth from "@/lib/auth" where session data needed
- Loading skeletons: <span className="inline-block w-16 h-5 bg-of-surface-container-high rounded animate-pulse" />
- Error display: {error && <p className="text-of-error text-xs">{error}</p>}
- Empty states: centered div with icon + message in text-of-on-surface-variant
- ALL colors via Obsidian Flux tokens (bg-of-*, text-of-*, border-of-*)
- Responsive: grid-cols-1 lg:grid-cols-2 for widget grids
- Section headers: text-[10px] font-bold uppercase tracking-wider text-of-on-surface-variant mb-3
  </action>
  <verify>
    <automated>cd /home/cristian/tiresias/portal && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - All 5 pages exist under portal/src/app/dashboard/aletheia/
    - Each page wrapped in TierGate requiredTier="enterprise"
    - Overview shows 4 widgets: chain health, tool timeline, sanitizer verdicts, policy violations
    - CoT Audit shows chain table, verify button, proof export, content viewer
    - Tool Activity shows timeline, command frequency, agent heatmap, deny/block log
    - Sanitizer shows verdict distribution, pattern frequency, blocked forensics
    - Policy Editor shows YAML editor, evaluation simulator, recent evaluations
    - All pages use useWidgetData for data fetching with proper loading/error states
    - TypeScript compiles without errors
    - No hardcoded hex colors -- all Obsidian Flux tokens
  </done>
</task>

</tasks>

<verification>
1. `cd /home/cristian/tiresias/portal && npx tsc --noEmit` -- zero TypeScript errors
2. `cd /home/cristian/tiresias/portal && npx next build 2>&1 | tail -20` -- build succeeds
3. Confirm sidebar renders Aletheia group: grep "aletheia" portal/src/components/dashboard/DashboardSidebar.tsx
4. Confirm all 5 page files exist: ls portal/src/app/dashboard/aletheia/*/page.tsx portal/src/app/dashboard/aletheia/page.tsx
5. Confirm TierGate wrapper in each page: grep -l "TierGate" portal/src/app/dashboard/aletheia/*/page.tsx portal/src/app/dashboard/aletheia/page.tsx
6. Confirm feature gate entries in backend: grep "aletheia" src/middleware/feature_gate.py shows all aletheia features mapped to "enterprise"
</verification>

<success_criteria>
- All /v1/aletheia/* endpoints gated to enterprise+ tier (verified via feature_gate.py inspection)
- Sidebar shows "Aletheia" nav group with 5 items for enterprise/mssp/saas tiers
- DashboardHeader shows Aletheia status indicator for enterprise+ tiers
- 5 Aletheia pages render with data from backend APIs via useWidgetData
- Sub-enterprise tiers see TierGate upgrade prompt on all Aletheia pages
- Portal builds successfully with next build
</success_criteria>

<output>
After completion, create `.planning/phases/17-aletheia-dashboard-tier-gating/17-01-SUMMARY.md`
</output>
