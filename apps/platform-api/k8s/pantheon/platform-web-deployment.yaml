# AUTO-RECONCILED from live cluster state on 2026-05-18.
# Cluster runs image :v0.1.122 deployed by CI/CD (this repo file was previously
# pinned at :v0.1.0 - drift created when CI applied newer manifests without
# updating the source file). This re-sync brings the repo back in line.
#
# Ephemeral fields stripped: status, managedFields, last-applied-configuration,
# autopilot.gke.io/resource-adjustment, deployment.kubernetes.io/revision,
# kubectl.kubernetes.io/restartedAt, creationTimestamp.
#
# Selector now includes app.kubernetes.io/part-of: pantheon (it was missing
# from the prior repo file but present on the cluster object - that is why
# kubectl apply was throwing "selector field is immutable").
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: platform-web
    app.kubernetes.io/component: web-frontend
    app.kubernetes.io/name: platform-web
    app.kubernetes.io/part-of: pantheon
  name: platform-web
  namespace: pantheon
spec:
  progressDeadlineSeconds: 600
  replicas: 2
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: platform-web
      app.kubernetes.io/part-of: pantheon
  strategy:
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: platform-web
        app.kubernetes.io/name: platform-web
        app.kubernetes.io/part-of: pantheon
    spec:
      containers:
      - env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              key: database-url-sync
              name: pantheon-secrets
        - name: SESSION_SECRET
          valueFrom:
            secretKeyRef:
              key: session-secret
              name: pantheon-secrets
        - name: WEB_PUBLIC_URL
          value: https://pantheon.saluca.com
        - name: API_PUBLIC_URL
          value: http://soulauth.pantheon.svc.cluster.local
        - name: SOULAUTH_INTERNAL_URL
          value: http://soulauth.pantheon.svc.cluster.local
        - name: MEMORY_SERVICE_URL
          value: http://memory-service.pantheon.svc.cluster.local:8910
        - name: MEMORY_SERVICE_KEY
          valueFrom:
            secretKeyRef:
              key: memory-service-key
              name: pantheon-secrets
        - name: COOKIE_DOMAIN
          value: pantheon.saluca.com
        - name: NODE_ENV
          value: production
        - name: BLOB_STORE_DRIVER
          valueFrom:
            secretKeyRef:
              key: blob-store-driver
              name: pantheon-secrets
              optional: true
        - name: BLOB_STORE_BUCKET
          valueFrom:
            secretKeyRef:
              key: blob-store-bucket
              name: pantheon-secrets
              optional: true
        - name: BLOB_STORE_ENDPOINT
          valueFrom:
            secretKeyRef:
              key: blob-store-endpoint
              name: pantheon-secrets
              optional: true
        - name: BLOB_STORE_REGION
          valueFrom:
            secretKeyRef:
              key: blob-store-region
              name: pantheon-secrets
              optional: true
        - name: BLOB_STORE_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              key: blob-store-access-key
              name: pantheon-secrets
              optional: true
        - name: BLOB_STORE_SECRET_KEY
          valueFrom:
            secretKeyRef:
              key: blob-store-secret-key
              name: pantheon-secrets
              optional: true
        - name: LLM_DEFAULT_PROVIDER
          value: anthropic
        - name: LLM_DEFAULT_MODEL
          value: claude-sonnet-4-6
        - name: LLM_RATE_LIMIT_RPM
          valueFrom:
            secretKeyRef:
              key: llm-rate-limit-rpm
              name: pantheon-secrets
              optional: true
        - name: ANTHROPIC_API_KEY
          valueFrom:
            secretKeyRef:
              key: anthropic-api-key
              name: pantheon-secrets
              optional: true
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              key: openai-api-key
              name: pantheon-secrets
              optional: true
        - name: COACH_MODEL
          valueFrom:
            secretKeyRef:
              key: coach-model
              name: pantheon-secrets
              optional: true
        - name: ANTHROPIC_COACH_MODEL
          valueFrom:
            secretKeyRef:
              key: anthropic-coach-model
              name: pantheon-secrets
              optional: true
        - name: VOICE_BUILDER_MODEL
          valueFrom:
            secretKeyRef:
              key: voice-builder-model
              name: pantheon-secrets
              optional: true
        image: us-central1-docker.pkg.dev/salucainfrastructure/pantheon/platform-web:v0.1.122
        imagePullPolicy: Always
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /
            port: http
            scheme: HTTP
          initialDelaySeconds: 15
          periodSeconds: 30
          successThreshold: 1
          timeoutSeconds: 5
        name: platform-web
        ports:
        - containerPort: 3000
          name: http
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /
            port: http
            scheme: HTTP
          initialDelaySeconds: 5
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 3
        resources:
          limits:
            cpu: '1'
            ephemeral-storage: 100Mi
            memory: 512Mi
          requests:
            cpu: 250m
            ephemeral-storage: 100Mi
            memory: 256Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          seccompProfile:
            type: RuntimeDefault
        startupProbe:
          failureThreshold: 12
          httpGet:
            path: /
            port: http
            scheme: HTTP
          initialDelaySeconds: 5
          periodSeconds: 5
          successThreshold: 1
          timeoutSeconds: 1
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        volumeMounts:
        - mountPath: /tmp
          name: tmp
        - mountPath: /app/.next/cache
          name: nextjs-cache
      - args:
        - --structured-logs
        - --auto-iam-authn
        - salucainfrastructure:us-central1:tiresias-db
        image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.14.3
        imagePullPolicy: IfNotPresent
        name: cloud-sql-proxy
        resources:
          limits:
            cpu: 200m
            ephemeral-storage: 1Gi
            memory: 128Mi
          requests:
            cpu: 50m
            ephemeral-storage: 1Gi
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          seccompProfile:
            type: RuntimeDefault
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext:
        fsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      serviceAccount: pantheon-sa
      serviceAccountName: pantheon-sa
      terminationGracePeriodSeconds: 30
      tolerations:
      - effect: NoSchedule
        key: kubernetes.io/arch
        operator: Equal
        value: amd64
      volumes:
      - emptyDir:
          medium: Memory
          sizeLimit: 64Mi
        name: tmp
      - emptyDir:
          sizeLimit: 256Mi
        name: nextjs-cache
