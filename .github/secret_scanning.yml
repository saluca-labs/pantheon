# GitHub Secret Scanning configuration
# Docs: https://docs.github.com/en/code-security/secret-scanning

# Push protection is managed at the organization/repo level.
# This file allows fine-grained pattern bypasses if needed.

# Example: bypass a specific pattern for a test file
# (Only add exceptions for clearly non-sensitive test data)
# omit_secret_scanning_alerts:
#   - pattern: test-session-secret
#     file: scripts/seed-admin.ts
